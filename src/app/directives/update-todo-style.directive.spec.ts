import { UpdateTodoStyleDirective } from './update-todo-style.directive';

describe('UpdateTodoStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateTodoStyleDirective();
    expect(directive).toBeTruthy();
  });
});
